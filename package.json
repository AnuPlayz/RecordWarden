{
  "name": "record-warden",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "@nomiclabs/hardhat-etherscan": "^3.1.7",
    "hardhat": "^2.17.3"
  },
  "scripts": {
    "compile": "npx hardhat compile",
    "source": "npx hardhat sourcify",
    "deploy": "npx hardhat run scripts/deploy.ts",
    "test": "npx hardhat run --network localhost scripts/test.ts",
    "abi": "npx thirdweb generate"
  },
  "dependencies": {
    "@thirdweb-dev/contracts": "^3.10.0"
  }
}
